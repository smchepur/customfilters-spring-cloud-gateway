spring:
  application:
    name: Custom Filters
  redis:
    host: localhost
    port: 6379

logging:
  level:
    ROOT: DEBUG
#    org.springframework.cloud.gateway: DEBUG
#    reactor.netty.http.client: DEBUG
server:
  port: 8084
management:
  endpoints:
    web:
      exposure:
        include=*: